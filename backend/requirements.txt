# Core Framework
fastapi
uvicorn[standard]
python-multipart

# AI & ML - Gemini SDK
google-genai
google-api-core

# Cloudflare R2 / S3-compatible storage (boto3 works with R2 via endpoint_url)
boto3
botocore

# PostgreSQL (job + history storage)
psycopg2-binary

# Redis + Celery (queue + caching)
redis
celery[redis]

# Text-to-Speech
edge-tts
pydub

# Video Processing
ffmpeg-python

# Utilities
python-dotenv
requests
aiofiles

# Authentication (Clerk JWT verification)
pyjwt[crypto]
cryptography
